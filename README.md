# A股市场情绪日报网页

这是“A股市场情绪日报”的公开只读网页版本，由服务器内部 SQL 数据增量生成。

公开网址：https://qrq3838.github.io/a-share-market-sentiment-dashboard/

## 更新逻辑

1. 服务器从内部 SQL 读取沪深个股成交、31 个申万一级行业成交额和四个指数收盘点位。
2. 首次运行建立自 2016-01-04 起的日度聚合归档；日常运行只重查最近 7 个自然日并增量追加。
3. 只有行业、个股成交聚合和四个指数全部完整时才采用该交易日；当天 16:30 前不会采用当日数据。
4. 服务器生成独立 `index.html` 和 `trends.html` 后提交到 GitHub，GitHub Pages 自动更新。
5. 数据日期和公开指标未变化时不会重复提交。

日报总览的市场拥挤度、四指数和行业趋势图展示最近60个完整交易日，日期按升序且不重复。四指数图包括上证指数、沪深300、创业板指和科创50，均以区间首日归一化为100。
`trends.html` 提供任意开始日期、结束日期和行业选择，覆盖全部完整归档历史；所有趋势图横轴均显示完整年份。

服务器数据源为内部 `qt_stk_daily`、`qt_sw_daily` 和 `qt_idx_daily` 日表。个股数据仅在服务器内存中计算拥挤度，公开文件不包含个股明细。

## 安全与口径

- 不上传原始工作簿、SQL 登录信息或服务器私钥。
- 不发布个股级数据，只发布聚合后的市场、指数和行业指标。
- GitHub 发布使用该仓库专属的服务器部署密钥，密钥不写入仓库。
- 页面仅供研究交流，不构成投资建议。

## 服务器更新

在服务器运行 `UpdateWebsiteFromSql.ps1` 会执行增量查询、完整性检查、网页生成并推送 GitHub。仅在需要重建全部历史聚合时使用 `-ForceFull`。
